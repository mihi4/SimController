# Arduino Part

SimController is the basic code I wrote to test everything.  
At the moment only some basic code for the RightAUX is there, since I started with it, the implementation of all the hardware modules is still missing.
  
On and on there will be new directories with only the code parts needed for that hardware, e.g. a SCRightAUX will hopefully be done shortly.
